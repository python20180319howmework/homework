'''
2. 定义一个函数:
   1)判断输入的字符串是否是回文字符串 （例如："上海自来水来自海上"为回文字符串）
'''

def huiwen(*arg):
	s = 0
	l = []
	for i in range(len(l)):
		l[0] == l[-1]:
		l.pop(0)
		l.pop()
		if i <= 1:
			s = "这是回文"	
		else :
			s = "这不是"
	return s

if __name__ == "__main__":
	l = input("请输入几个随便什么")
	l1 = type(l)
	print(huiwen(l1))



























